<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Close exports that never recorded an outcome (worker crash or a
             failed terminal audit write) with a completion_unknown event.
             Retention runs through @api.autovacuum on the model. -->
        <record id="ir_cron_pdf_export_reconcile" model="ir.cron">
            <field name="name">PDF Export: reconcile unfinished exports</field>
            <field name="model_id" ref="model_dashboard_pdf_export_log"/>
            <field name="state">code</field>
            <field name="code">model._cron_reconcile()</field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
        </record>
    </data>
</odoo>
