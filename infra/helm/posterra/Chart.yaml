apiVersion: v2
name: posterra
description: Earlyread SaaS — Odoo 19 platform (posterra_portal + dashboard_builder)
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# The Odoo major this chart targets.
appVersion: "19.0"
